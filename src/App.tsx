export default function App() {
  return (
    <main className="app-shell">
      <section className="hero-card">
        <p className="eyebrow">Taskflow Kanban</p>
        <h1>Plan work across focused delivery lanes.</h1>
        <p className="description">
          A lightweight kanban board for tracking tasks, ownership, and
          handoff status. Core workflow features land in the next commits.
        </p>
      </section>
    </main>
  );
}
